# Python dependencies for KiCad NLP Auto-Design Skill

# Core dependencies (no external KiCad API required)
# This skill uses native S-expression format generation
# No KiCad installation needed for file generation

# Data processing
dataclasses>=0.6  # Python 3.7+ has built-in, but explicit for type hints
typing-extensions>=4.0.0  # Extended typing support

# Optional dependencies for enhanced features
# Uncomment if needed:
# openai>=1.0.0  # For AI-powered natural language understanding
# pandas>=1.3.0  # For BOM generation and data export

# Development dependencies (for testing)
pytest>=7.0.0  # Testing framework
black>=22.0.0  # Code formatter
mypy>=0.900  # Type checker
pylint>=2.15.0  # Code linter
